<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Hatiralari sesli kaydetmek icin -->
    <uses-permission android:name="android.permission.RECORD_AUDIO"/>
    <!-- Whisper modelini bir kereligine indirmek icin -->
    <uses-permission android:name="android.permission.INTERNET"/>
    <!-- Kayit sirasinda ekranin kapanmamasi icin -->
    <uses-permission android:name="android.permission.WAKE_LOCK"/>
    <!-- Guncellemeyi yalnizca sayacsiz aglarda (Wi-Fi) indirmek icin -->
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>
    <!-- Yeni surumu kendi kendine kurabilmek icin. Uygulama magazasi
         olmadigi icin guncellemeyi uygulamanin kendisi kuruyor; kullanici
         yine de sistemin onay penceresini gorur. -->
    <uses-permission android:name="android.permission.REQUEST_INSTALL_PACKAGES"/>
    <!-- Fotograf cekmek icin (kamera opsiyoneldir, cihazda yoksa da kurulur) -->
    <uses-permission android:name="android.permission.CAMERA"/>
    <uses-feature android:name="android.hardware.camera" android:required="false"/>
    <uses-feature android:name="android.hardware.camera.autofocus" android:required="false"/>
    <uses-feature android:name="android.hardware.microphone" android:required="false"/>

    <application
        android:label="hatırlaf"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher"
        android:requestLegacyExternalStorage="false">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <!-- PackageInstaller kurulum sonucunu buraya bildirir.
             exported=false: yalnizca sistem ve biz tetikleyebiliriz. -->
        <receiver
            android:name=".KurulumAlicisi"
            android:exported="false"/>

        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
    </application>

    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
        <!-- image_picker: kamera uygulamasini bulabilmek icin -->
        <intent>
            <action android:name="android.media.action.IMAGE_CAPTURE"/>
        </intent>
    </queries>
</manifest>
